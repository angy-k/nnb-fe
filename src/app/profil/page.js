import ProfileComponent from  '@/components/Profile'

const Profile = () => {
  return (
    <div className="mt-60 w-full grid place-items-center" style={{background: 'linear-gradient(to bottom, #261A54 30%, #f0f0f0 30%)'}}>
      <ProfileComponent />
    </div>
  )
}

export default Profile;
